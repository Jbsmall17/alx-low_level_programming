#include "main.h"

/**
 * int _strlen - returns the kength of a string
 * @s: a pointer to s
 * Return: returns a number
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}
	return (i);
}	
